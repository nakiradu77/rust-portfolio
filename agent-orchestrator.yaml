port: 3000
defaults:
  runtime: tmux
  agent: codex
  workspace: worktree
  notifiers:
    - desktop
projects:
  rust-portfolio:
    name: rust-portfolio
    repo: nakiradu77/rust-portfolio
    path: /home/maxim/RustroverProjects/rust-portfolio
    defaultBranch: trunk
    sessionPrefix: rp
    scm:
      plugin: github
    tracker:
      plugin: github
    postCreate:
      - npm install
 # Codex-specific configuration
    agentConfig:
      prfile: Flash
      permissions: default
